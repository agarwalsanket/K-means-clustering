# K-means-clustering
Python code for K-means clustering (k = 1 to 20) and reiterating 1000 times on each k-means 
(for selecting random centroids initially 1000 times for each k-means) for better accuracy.

Hard coding the threshold difference between two centroids to be 0.1
